{
  "name": "@watchtogether/extension",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.38",
    "esbuild": "^0.24.0",
    "sharp": "^0.34.5",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
