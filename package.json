{
  "name": "watchtogether",
  "private": true,
  "workspaces": [
    "server",
    "extension",
    "shared"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "typecheck": "tsc --noEmit --project server/tsconfig.json && tsc --noEmit --project extension/tsconfig.json"
  }
}
