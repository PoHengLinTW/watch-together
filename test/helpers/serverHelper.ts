/**
 * Start a test WebSocket server on a random port.
 * Implementation will be filled in during Phase 2 (server integration).
 */
export async function startTestServer(
  port?: number,
): Promise<{ port: number; close: () => Promise<void> }> {
  throw new Error('Not implemented — will be implemented in Phase 2');
}
